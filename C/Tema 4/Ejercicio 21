/* Algoritmo que sume los elementos múltiplos de tres de un vector t de N reales.   */
#include <stdio.h>
#define K 5


int main() {

int i, sum;
int t[K]={1,2,3,4,9};

sum=0;
for(i=0;i<K;i++){
        if(t[i]%3==0)
    sum = sum+t[i];
}
printf("Suma de los multiplos de 3: %d",sum);
return 0;
}
